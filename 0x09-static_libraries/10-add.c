#include "main.h"

/**
  * add - to add two integers and returns the result
  *@x: first integer
  *@y: second integer
  *
  * Return: add success
  */

int add(int x, int y)
{

	int add = 0;

	add = x + y;

	return (add);

}
