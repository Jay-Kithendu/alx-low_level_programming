#include "main.h"

/**
  * main - prints the number of arguments passed into it
  * @argc: counter
  * @argv: vetor
  *
  * Return: 0 on success
  */

int main(int argc, char *argv[])
{
	int i;
	int sum;

	if (argc > 1)
		sum += 1;
	printf("%d\n", sum);
	return (0);
}
